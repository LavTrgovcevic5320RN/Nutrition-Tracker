<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.raf.projekat_jun_lav_trgovcevic_53_2020rn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NutritionTracker"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentation.view.activities.FilterMealsActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.view.activities.MealActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.view.activities.MainActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.view.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.view.activities.LoginActivity"
            android:exported="true" />
    </application>

</manifest>